# Windows 배치 파일은 binary로 처리하여 ANSI(CP949) 인코딩 유지
*.bat binary

# Python 파일은 UTF-8
*.py text eol=lf

# Markdown 파일은 UTF-8
*.md text eol=lf

# 텍스트 파일 기본 설정
*.txt text eol=crlf

# Excel 파일은 바이너리
*.xlsx binary
